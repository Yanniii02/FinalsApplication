<p style="color: green"><%= notice %></p>

<h1>Driver Details</h1>

<p>
  <strong>Name:</strong> <%= @driver.name %><br>
  <strong>Contact Number:</strong> <%= @driver.contact_number %><br>
  <strong>Bus:</strong> <%= @driver.bus.plate_number %>
</p>

<%= link_to 'Edit', edit_driver_path(@driver) %> |
<%= link_to 'Back', drivers_path %>
<%= button_to 'Destroy', @driver, method: :delete, data: { confirm: 'Are you sure?' } %>