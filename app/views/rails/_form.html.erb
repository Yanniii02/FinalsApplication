<%= form_with(model: rail) do |form| %>
  <% if rail.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(rail.errors.count, "error") %> prohibited this rail from being saved:</h2>

      <ul>
        <% rail.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :Reservation, style: "display: block" %>
    <%= form.text_field :Reservation %>
  </div>

  <div>
    <%= form.label :passenger_id, style: "display: block" %>
    <%= form.text_field :passenger_id %>
  </div>

  <div>
    <%= form.label :trip_id, style: "display: block" %>
    <%= form.text_field :trip_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
